class devices {
    constructor( id, name, place, command, color) {
      this.id = id;
      this.name = name;
      this.place = place;
      this.command = command;
      this.color = color;
    }
  }

const devicesList = [

];

export { devices, devicesList };